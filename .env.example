# Postgres Communs prod (target ETL — schema schema_commun_v2 only)
DATABASE_URL=postgres://user:password@host:port/database

# Prefect server (separate Scalingo app)
PREFECT_API_URL=http://etl-nightly-prefect-server.osc-fr1.scalingo.io/api

# LLM / embeddings
ANTHROPIC_API_KEY=sk-ant-...
VOYAGE_API_KEY=pa-...

# Scalingo CLI (for snapshot creation in flows)
SCALINGO_API_TOKEN=
SCALINGO_APP_COMMUNS=communs-prod
